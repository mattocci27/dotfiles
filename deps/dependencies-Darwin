#!/usr/bin/env bash
set -e

# command line tools
if [ ! -e /usr/bin/xcode-select ]; then
  sudo xcode-select --install
fi

# homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

/opt/homebrew/bin/brew bundle --file ./deps/Brewfile -v
